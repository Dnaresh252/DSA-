#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	int a[n];
	for(int i=0;i<n;i++)
	{
		cin>>a[i];
	}
	vector<int>l;
	int max=0;
	for(int i=n-1;i>=0;i--)
	{
		if(a[i]>max)
		{
			max=a[i];
			l.push_back(a[i]);
		}
	}
	for(int i=0;i<l.size();i++)
	{	
	cout<<l[i];
}

}
